// Nothing talks to Firestore but the API's service account, which bypasses
// these rules. Every client SDK request is refused.
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} { allow read, write: if false; }
  }
}
